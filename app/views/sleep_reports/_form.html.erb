<%= bootstrap_form_with(model: sleep_report, local: true) do |form| %>
  <%= form.datetime_select :asleep_at, ampm: true, include_blank: true %>
  <%= form.datetime_select :wakeup_at, ampm: true, include_blank: true %>
  <%= form.submit class: "btn btn-primary" %>
<% end %>
